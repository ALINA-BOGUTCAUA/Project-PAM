<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectpam">

    <!-- Разрешение для работы с датчиками шагомера (Android 10+) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectPAM">

        <!-- Главная активность -->
        <activity android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.RegisterActivity" />
        <activity android:name=".view.LoginActivity" android:exported="true"/>
        <activity android:name=".view.AccountActivity" android:exported="true"/>





        <!-- Дополнительно можно добавить другие активности, например: -->
        <!--
        <activity android:name=".view.RecommendationsActivity" android:exported="true"/>
        <activity android:name=".view.AccountActivity" android:exported="true"/>
        -->

    </application>

</manifest>
